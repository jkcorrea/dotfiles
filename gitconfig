[user]
	email = jcorrea257@gmail.com
	name = Jake Correa
	signingkey = 42F322AA038B33DC
[push]
	default = simple
[core]
	excludesfile = /Users/jkcorrea/.gitignore_global
	editor = code --wait
[gc]
	auto = 256
[color]
	status = always
[diff]
	tool = default-difftool
[difftool "default-difftool"]
	cmd = code --wait --diff $LOCAL $REMOTE
[alias]
	get = gerrit get
	repo = gerrit repo
	cleanup = gerrit cleanup
	mail = gerrit mail
	pending = gerrit pending
	change = gerrit change
	sync = gerrit sync
	gofmt = gerrit gofmt
	rebase-work = gerrit rebase-work
	submit = gerrit submit
	commit = gerrit commit
[gpg]
	program = /usr/local/bin/gpg
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
